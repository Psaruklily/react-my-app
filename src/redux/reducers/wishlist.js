const initialState = {
    wishlist: []
};

const wishlist = (state = initialState, action) => {
    switch(action.type) {
        default: {
            return state;
        }
    }
}

export default wishlist;